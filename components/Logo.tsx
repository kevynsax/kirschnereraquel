export const Logo = () => {
    return (
        <div className="logo" onClick={() => window.location.href = `/`}>
            <span>R</span>
            <span>&</span>
            <span>K</span>
        </div>
    );
}
